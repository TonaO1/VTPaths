# Copy to .env.local and fill in. Never commit .env.local.
# The same three keys go in Vercel: Project Settings -> Environment Variables.
# Vite only exposes vars prefixed VITE_ to the browser.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_MAPBOX_TOKEN=
